---
import GamesLayout from '../../layouts/GamesLayout.astro';
---

<GamesLayout title="Teru" description="Teru — an emotional 3D platformer by Quiet Atlas. Two weather caretakers restore environmental balance. Healing through service and quiet compassion.">
  <!-- Hero -->
  <section class="game-hero">
    <div class="game-hero-visual teru-bg">
      <div class="game-hero-overlay"></div>
      <div class="game-hero-content container fade-in">
        <span class="game-hero-label">Emotional 3D Platformer · Environmental Restoration</span>
        <h1 class="game-hero-title">Teru</h1>
        <p class="game-hero-tagline">They do not fight the world. They restore it.</p>
      </div>
    </div>
  </section>

  <!-- About -->
  <section class="game-about container">
    <div class="content-width">
      <div class="game-lead fade-in">
        <p class="lead-text">
          Two celestial weather caretakers restore environmental balance by delivering
          <strong>teru teru bōzu</strong> to communities below. <strong>Sun Girl</strong> delivers
          sunshine; <strong>Rain Boy</strong> delivers rain. They do not fight the world — they restore it.
        </p>
      </div>

      <div class="pull-quote fade-in">
        "Healing through service. Restoring balance. Processing grief through care."
      </div>

      <div class="game-details fade-in">
        <h2>Emotional Experience</h2>
        <p>
          Teru explores healing through service, restoring balance, quiet compassion, processing
          grief through care, and emotional growth through helping others. Weather delivery is
          environmental care, not symbolism — the player serves communities.
        </p>

        <h2>Structure</h2>
        <p>
          <strong>Ark of Teru (Hub)</strong> — A floating sanctuary with sunny and rainy cloud mounds,
          day/night cycle, memory exchange bridge, NPC caretakers, and quiet reflection space.
        </p>
        <p>
          <strong>Levels</strong> — Nine levels per character, with increasing emotional weight and
          grounded environmental needs: drought, growth, recovery.
        </p>
        <p>
          <strong>Memory Collectibles</strong> — Each character gathers items belonging to the other.
          Exchanging them unlocks emotional dialogues.
        </p>
        <p>
          <strong>Hidden Redemption Level</strong> — After collecting all items, one tragedy can be
          undone. Only one per playthrough. An emotional final scene.
        </p>
      </div>

      <div class="game-features stagger-children">
        <div class="feature-card card">
          <h3>Ark of Teru</h3>
          <p>Floating hub with sun and rain, memory exchange, and space to breathe.</p>
        </div>
        <div class="feature-card card">
          <h3>Memory Exchange</h3>
          <p>Collect items that belong to the other; exchange them for emotional dialogue.</p>
        </div>
        <div class="feature-card card">
          <h3>One Redemption</h3>
          <p>Complete the set and one tragedy can be undone — once per playthrough.</p>
        </div>
      </div>

      <div class="signature fade-in">
        <p><strong>Signature identity:</strong> A platformer about restoring balance instead of conquering obstacles.</p>
      </div>
    </div>
  </section>

  <!-- Info -->
  <section class="game-info container">
    <div class="info-grid fade-in">
      <div class="info-block">
        <h3>Status</h3>
        <p>In Development</p>
      </div>
      <div class="info-block">
        <h3>Genre</h3>
        <p>Emotional 3D Platformer</p>
      </div>
      <div class="info-block">
        <h3>Platform</h3>
        <p>PC / Mac</p>
      </div>
      <div class="info-block">
        <h3>Engine</h3>
        <p>Godot</p>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="cta container">
    <div class="cta-inner fade-in">
      <hr class="ornamental" />
      <p class="cta-text">Teru is being carefully folded into existence. We'll share more when the light is ready.</p>
      <a href="https://quietatlas.itch.io/" target="_blank" rel="noopener noreferrer" class="btn btn-large">Follow on itch.io</a>
    </div>
  </section>
</GamesLayout>

<style>
  /* ─── Game Hero ─── */
  .game-hero {
    margin-top: calc(-1 * var(--nav-height));
  }

  .game-hero-visual {
    min-height: 60vh;
    display: flex;
    align-items: flex-end;
    position: relative;
    padding: var(--space-2xl) 0 var(--space-xl);
  }

  .teru-bg {
    background: linear-gradient(160deg, #0f0a1a 0%, #1a0f2e 30%, #2d1b4e 60%, #4a1942 100%);
  }

  .game-hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to bottom, rgba(8, 8, 12, 0.3) 0%, rgba(8, 8, 12, 0.9) 100%);
  }

  .game-hero-content {
    position: relative;
    z-index: 1;
  }

  .game-hero-label {
    display: inline-block;
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 0.85rem;
    color: var(--accent);
    margin-bottom: var(--space-md);
  }

  .game-hero-title {
    font-size: clamp(3.5rem, 10vw, 7rem);
    line-height: 0.95;
    letter-spacing: -0.04em;
    margin-bottom: var(--space-md);
  }

  .game-hero-tagline {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: clamp(1rem, 2.5vw, 1.35rem);
    color: var(--text-secondary);
    max-width: 500px;
    margin: 0;
  }

  /* ─── Game About ─── */
  .game-about {
    padding: var(--space-2xl) 0;
  }

  .lead-text {
    font-family: var(--font-serif);
    font-size: 1.15rem;
    line-height: 1.8;
    color: var(--text-secondary);
  }

  .game-details {
    margin-top: var(--space-xl);
  }

  .game-details h2 {
    font-size: 1.75rem;
    margin-bottom: var(--space-md);
  }

  .game-details p {
    font-family: var(--font-serif);
    font-size: 1rem;
    line-height: 1.8;
    color: var(--text-secondary);
  }

  .signature {
    margin-top: var(--space-xl);
    padding: var(--space-md);
    border-left: 3px solid var(--accent);
    background: var(--bg-alt);
    border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
  }

  .signature p {
    font-family: var(--font-serif);
    font-size: 1rem;
    line-height: 1.7;
    color: var(--text-secondary);
    margin: 0;
  }

  /* ─── Features ─── */
  .game-features {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-md);
    margin-top: var(--space-xl);
  }

  .feature-card h3 {
    font-size: 1rem;
    margin-bottom: var(--space-xs);
  }

  .feature-card p {
    font-family: var(--font-serif);
    font-size: 0.88rem;
    line-height: 1.6;
    color: var(--text-secondary);
    margin: 0;
  }

  @media (max-width: 768px) {
    .game-features {
      grid-template-columns: 1fr;
    }
  }

  /* ─── Info Grid ─── */
  .game-info {
    padding: var(--space-xl) 0;
  }

  .info-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-md);
    padding: var(--space-lg);
    background: var(--bg-alt);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
  }

  .info-block h3 {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--text-muted);
    margin-bottom: var(--space-xs);
  }

  .info-block p {
    font-family: var(--font-serif);
    font-size: 0.95rem;
    color: var(--text);
    margin: 0;
  }

  @media (max-width: 768px) {
    .info-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  /* ─── CTA ─── */
  .cta {
    padding: var(--space-xl) 0 var(--space-2xl);
  }

  .cta-inner {
    text-align: center;
  }

  .cta-text {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 1.1rem;
    color: var(--text-secondary);
    margin-bottom: var(--space-lg);
  }
</style>
