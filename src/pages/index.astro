---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Home">
  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-inner container">
      <div class="hero-content fade-in">
        <div class="hero-date">Est. 2025 • Ontario, Canada</div>
        <h1 class="hero-title">Quiet Atlas</h1>
        <p class="hero-subtitle">An indie label for healing-oriented, contemplative games.</p>
        <div class="hero-tagline">
          <span class="hero-tagline-text">Games that breathe.</span>
        </div>
      </div>
      
      <div class="hero-logo float">
        <img src="/logo.png" alt="Quiet Atlas Globe" />
      </div>
    </div>
    
    <div class="hero-scroll">
      <span>Scroll</span>
      <div class="scroll-line"></div>
    </div>
  </section>

  <!-- Philosophy Section -->
  <section class="philosophy container">
    <div class="section-header">
      <span>Our Philosophy</span>
    </div>
    
    <div class="philosophy-content content-width">
      <p class="drop-cap fade-in">
        We believe games can be a place of rest. Not escape—rest. A space where you're invited to slow down, 
        to feel without explanation, to remember something you didn't know you'd forgotten.
      </p>
      
      <div class="pull-quote fade-in">
        "A place that helps people remember how to be soft."
      </div>
      
      <p class="fade-in">
        Quiet Atlas games are gentle but not shallow. Emotionally sincere without being melodramatic. 
        We treat childlike belief with adult respect—because wonder doesn't need to be outgrown.
      </p>
      
      <p class="fade-in">
        Low UI. Low friction. High atmosphere.
      </p>
    </div>
  </section>

  <!-- Featured Section -->
  <section class="featured container">
    <div class="section-header">
      <span>Now Playing</span>
    </div>
    
    <article class="featured-article fade-in">
      <div class="featured-header">
        <span class="featured-label">Featured Release</span>
        <h2 class="featured-title">Bullet Well</h2>
        <p class="byline">A quirky arcade experiment in doodle-style chaos</p>
      </div>
      
      <div class="featured-body">
        <p>
          Sometimes healing comes from the most unexpected places. Bullet Well blends the 
          vertical momentum of Doodle Jump with the beautiful chaos of bullet hell—rendered 
          in a playful scribble aesthetic that feels like notebook margins come to life.
        </p>
        
        <a href="/games" class="btn">Read More</a>
      </div>
    </article>
  </section>

  <!-- Call to Action -->
  <section class="cta container">
    <div class="cta-inner fade-in">
      <hr class="ornamental" />
      <p class="cta-text">
        Interested in what we're creating?
      </p>
      <a href="https://quietatlas.itch.io/" target="_blank" rel="noopener noreferrer" class="btn">
        Visit us on itch.io
      </a>
    </div>
  </section>
</Layout>

<style>
  /* Hero Section */
  .hero {
    min-height: calc(100vh - 70px);
    display: flex;
    flex-direction: column;
    justify-content: center;
    position: relative;
    padding: var(--space-xl) 0;
  }
  
  .hero-inner {
    display: grid;
    grid-template-columns: 1fr auto;
    align-items: center;
    gap: var(--space-xl);
  }
  
  .hero-date {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 0.85rem;
    color: var(--ink-faded);
    margin-bottom: var(--space-md);
    letter-spacing: 0.05em;
  }
  
  .hero-title {
    font-size: clamp(3rem, 10vw, 7rem);
    margin-bottom: var(--space-md);
    line-height: 0.95;
  }
  
  .hero-subtitle {
    font-size: clamp(1rem, 2.5vw, 1.35rem);
    color: var(--ink-light);
    max-width: 500px;
    margin-bottom: var(--space-lg);
  }
  
  .hero-tagline {
    display: inline-block;
    padding: var(--space-sm) var(--space-lg);
    border: 1px solid var(--ink);
    position: relative;
  }
  
  .hero-tagline-text {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 1.1rem;
  }
  
  .hero-logo {
    width: clamp(150px, 20vw, 280px);
    opacity: 0.9;
  }
  
  .hero-logo img {
    width: 100%;
    height: auto;
    border-radius: 50%;
    filter: grayscale(20%);
  }
  
  .hero-scroll {
    position: absolute;
    bottom: var(--space-xl);
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-sm);
    color: var(--ink-faded);
    font-family: var(--font-serif);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.2em;
  }
  
  .scroll-line {
    width: 1px;
    height: 40px;
    background: linear-gradient(to bottom, var(--ink-faded), transparent);
    animation: scrollPulse 2s ease-in-out infinite;
  }
  
  @keyframes scrollPulse {
    0%, 100% { opacity: 0.3; transform: scaleY(1); }
    50% { opacity: 1; transform: scaleY(1.2); }
  }
  
  @media (max-width: 768px) {
    .hero-inner {
      grid-template-columns: 1fr;
      text-align: center;
    }
    
    .hero-logo {
      order: -1;
      margin: 0 auto;
    }
    
    .hero-subtitle {
      margin-left: auto;
      margin-right: auto;
    }
    
    .hero-scroll {
      display: none;
    }
  }
  
  /* Philosophy Section */
  .philosophy {
    padding-top: var(--space-2xl);
    padding-bottom: var(--space-2xl);
  }
  
  .philosophy-content p {
    font-size: 1.1rem;
  }
  
  /* Featured Section */
  .featured {
    padding-top: var(--space-xl);
    padding-bottom: var(--space-2xl);
  }
  
  .featured-article {
    max-width: 800px;
    margin: 0 auto;
    background: var(--paper-aged);
    border: 1px solid var(--ink-faded);
    padding: var(--space-xl);
    position: relative;
  }
  
  .featured-article::before {
    content: '';
    position: absolute;
    top: 10px;
    left: 10px;
    right: -10px;
    bottom: -10px;
    border: 1px solid var(--ink-faded);
    z-index: -1;
  }
  
  .featured-header {
    text-align: center;
    margin-bottom: var(--space-lg);
    padding-bottom: var(--space-lg);
    border-bottom: 1px solid var(--ink-faded);
  }
  
  .featured-label {
    display: inline-block;
    font-family: var(--font-serif);
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--ink-faded);
    margin-bottom: var(--space-sm);
  }
  
  .featured-title {
    font-size: clamp(2rem, 5vw, 3.5rem);
    margin-bottom: var(--space-sm);
  }
  
  .featured-body {
    text-align: center;
  }
  
  .featured-body p {
    max-width: 100%;
    margin-left: auto;
    margin-right: auto;
    margin-bottom: var(--space-lg);
  }
  
  /* CTA Section */
  .cta {
    padding-top: var(--space-xl);
    padding-bottom: var(--space-2xl);
  }
  
  .cta-inner {
    text-align: center;
  }
  
  .cta-text {
    font-family: var(--font-serif);
    font-size: 1.25rem;
    margin-bottom: var(--space-lg);
    color: var(--ink-light);
  }
</style>
