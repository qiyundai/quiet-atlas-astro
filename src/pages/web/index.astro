---
import WebLayout from '../../layouts/WebLayout.astro';
---

<WebLayout title="Home" description="Quiet Atlas builds calm, deliberately crafted static websites that last. Astro-powered, git-based, deployed on Cloudflare.">
  <!-- Hero -->
  <section class="hero">
    <div class="hero-inner container">
      <div class="hero-content fade-in">
        <span class="section-label">Web Development Studio</span>
        <h1 class="hero-title">Websites that work quietly and last.</h1>
        <p class="hero-desc">
          We craft static, low-maintenance websites for businesses that value
          reliability over trends. Clean architecture, git-based content, deployed
          to the edge.
        </p>
        <div class="hero-actions">
          <a href="/web/services" class="btn btn-primary btn-large">Our Services</a>
          <a href="/web/contact" class="btn btn-large">Get in Touch</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Principles -->
  <section class="principles container">
    <div class="section-header">
      <span>What We Stand For</span>
    </div>

    <div class="principles-grid stagger-children">
      <div class="principle-card card">
        <div class="principle-icon">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2" /><path d="M8 21h8" /><path d="M12 17v4" /></svg>
        </div>
        <h3>Static Architecture</h3>
        <p>No runtime servers. No database queries. Just fast, pre-built pages served from the edge. Your site loads in milliseconds, not seconds.</p>
      </div>

      <div class="principle-card card">
        <div class="principle-icon">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3" /><path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42" /></svg>
        </div>
        <h3>Low Maintenance</h3>
        <p>Your content lives in Git. No plugin updates, no security patches, no midnight emergencies. Update content, push, and it's live.</p>
      </div>

      <div class="principle-card card">
        <div class="principle-icon">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" /></svg>
        </div>
        <h3>Built to Last</h3>
        <p>No framework churn. No vendor lock-in. Your site is built on open standards and will work years from now, exactly as intended.</p>
      </div>
    </div>
  </section>

  <!-- Featured Work -->
  <section class="featured container">
    <div class="section-header">
      <span>Recent Work</span>
    </div>

    <div class="featured-card fade-in">
      <div class="featured-content">
        <span class="featured-badge">In Deployment</span>
        <h2>Sunny Nails Spa</h2>
        <p>
          A calm, inviting web presence for a local spa business. Built with the same
          static architecture we champion — fast, reliable, and easy to maintain.
        </p>
        <a href="/web/work" class="btn">View Our Work</a>
      </div>
      <div class="featured-visual">
        <div class="featured-placeholder">
          <span>SN</span>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="cta container">
    <div class="cta-inner fade-in">
      <h2>Ready to build something lasting?</h2>
      <p>We'd love to hear about your project. No pressure, no jargon — just a conversation about what you need.</p>
      <a href="/web/contact" class="btn btn-primary btn-large">Start a Conversation</a>
    </div>
  </section>
</WebLayout>

<style>
  /* ─── Hero ─── */
  .hero {
    padding: var(--space-2xl) 0 var(--space-xl);
  }

  .hero-title {
    font-size: clamp(2.5rem, 6vw, 4.25rem);
    line-height: 1.08;
    letter-spacing: -0.03em;
    margin-top: var(--space-md);
    margin-bottom: var(--space-lg);
  }

  .hero-desc {
    font-size: 1.1rem;
    line-height: 1.7;
    max-width: 560px;
    margin-bottom: var(--space-xl);
  }

  .hero-actions {
    display: flex;
    gap: var(--space-md);
    flex-wrap: wrap;
  }

  /* ─── Principles ─── */
  .principles {
    padding: var(--space-xl) 0 var(--space-2xl);
  }

  .principles-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
  }

  .principle-card {
    text-align: left;
  }

  .principle-icon {
    color: var(--accent);
    margin-bottom: var(--space-md);
  }

  .principle-card h3 {
    font-size: 1.1rem;
    margin-bottom: var(--space-sm);
  }

  .principle-card p {
    font-size: 0.92rem;
    line-height: 1.65;
    margin: 0;
  }

  @media (max-width: 768px) {
    .principles-grid {
      grid-template-columns: 1fr;
    }
  }

  /* ─── Featured ─── */
  .featured {
    padding: var(--space-xl) 0 var(--space-2xl);
  }

  .featured-card {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-xl);
    align-items: center;
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
  }

  .featured-badge {
    display: inline-block;
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--accent);
    background: var(--accent-subtle);
    padding: 0.3rem 0.75rem;
    border-radius: var(--radius-sm);
    margin-bottom: var(--space-md);
  }

  .featured-content h2 {
    font-size: 1.75rem;
    margin-bottom: var(--space-sm);
  }

  .featured-content p {
    font-size: 0.95rem;
    line-height: 1.65;
    margin-bottom: var(--space-lg);
  }

  .featured-visual {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .featured-placeholder {
    width: 100%;
    aspect-ratio: 4/3;
    background: var(--bg-alt);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-display);
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--text-muted);
    letter-spacing: -0.02em;
  }

  @media (max-width: 768px) {
    .featured-card {
      grid-template-columns: 1fr;
    }

    .featured-visual {
      order: -1;
    }
  }

  /* ─── CTA ─── */
  .cta {
    padding: var(--space-xl) 0 var(--space-2xl);
  }

  .cta-inner {
    text-align: center;
    max-width: 560px;
    margin: 0 auto;
  }

  .cta-inner h2 {
    font-size: clamp(1.75rem, 4vw, 2.25rem);
    margin-bottom: var(--space-md);
  }

  .cta-inner p {
    font-size: 1rem;
    margin-bottom: var(--space-lg);
  }
</style>
