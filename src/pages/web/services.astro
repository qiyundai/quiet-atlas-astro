---
import WebLayout from '../../layouts/WebLayout.astro';
---

<WebLayout title="Services" description="Web development services from Quiet Atlas — static sites, CMS integration, Cloudflare deployment, and ongoing maintenance.">
  <!-- Page Header -->
  <section class="page-header">
    <div class="container">
      <span class="section-label fade-in">What We Offer</span>
      <h1 class="fade-in">Services built around reliability.</h1>
      <p class="page-intro fade-in">
        Every service we offer follows the same philosophy: build it right, build it to last,
        and make it easy to maintain. No complexity for its own sake.
      </p>
    </div>
  </section>

  <!-- Services Grid -->
  <section class="services container">
    <div class="services-grid stagger-children">
      <article class="service-card card">
        <div class="service-number">01</div>
        <h2>Static Website Development</h2>
        <p>
          We build websites with Astro — a modern framework that produces zero-JavaScript static pages
          by default. Your visitors get instant load times. You get a site that simply works.
        </p>
        <ul class="service-features">
          <li>Astro-powered static generation</li>
          <li>Semantic, accessible HTML</li>
          <li>Responsive design</li>
          <li>Lighthouse scores of 95+</li>
        </ul>
      </article>

      <article class="service-card card">
        <div class="service-number">02</div>
        <h2>Content Management</h2>
        <p>
          Content lives in Git via TinaCMS — a visual editor backed by version control. No databases
          to maintain, no security patches to chase. Edit visually, commit to Git, done.
        </p>
        <ul class="service-features">
          <li>TinaCMS visual editing</li>
          <li>Git-based version control</li>
          <li>No database dependency</li>
          <li>Content history & rollbacks</li>
        </ul>
      </article>

      <article class="service-card card">
        <div class="service-number">03</div>
        <h2>Hosting & Deployment</h2>
        <p>
          Your site deploys to Cloudflare Pages — served from 300+ edge locations worldwide.
          Push a change to Git, and it's live in seconds. Globally. Automatically.
        </p>
        <ul class="service-features">
          <li>Cloudflare Pages deployment</li>
          <li>Global CDN distribution</li>
          <li>Automatic SSL certificates</li>
          <li>Git-triggered deploys</li>
        </ul>
      </article>

      <article class="service-card card">
        <div class="service-number">04</div>
        <h2>Ongoing Maintenance</h2>
        <p>
          After launch, we're still here. We offer maintenance tiers to keep your site
          healthy — from quarterly check-ins to proactive monitoring and content updates.
        </p>
        <ul class="service-features">
          <li>Performance monitoring</li>
          <li>Content update support</li>
          <li>Security oversight</li>
          <li>Quarterly health reports</li>
        </ul>
      </article>
    </div>
  </section>

  <!-- Approach -->
  <section class="approach container">
    <div class="section-header">
      <span>Our Approach</span>
    </div>

    <div class="approach-content content-width fade-in">
      <h2>Boutique craftsmanship, not factory output.</h2>
      <p>
        We take on a small number of projects at a time. Each one gets our full attention.
        We don't hand you a template — we craft something tailored to your specific needs,
        built on a foundation that won't need replacing in two years.
      </p>
      <p>
        Our stack is deliberately simple: Astro for building, TinaCMS for content,
        Cloudflare for hosting. Three tools, proven and reliable. Nothing more than
        what's needed.
      </p>
    </div>
  </section>

  <!-- CTA -->
  <section class="cta container">
    <div class="cta-inner fade-in">
      <h2>Have a project in mind?</h2>
      <p>Let's talk about what you need — and whether we're the right fit.</p>
      <a href="/web/contact" class="btn btn-primary btn-large">Get in Touch</a>
    </div>
  </section>
</WebLayout>

<style>
  /* ─── Page Header ─── */
  .page-header {
    padding: var(--space-2xl) 0 var(--space-xl);
  }

  .page-header h1 {
    margin-top: var(--space-md);
    font-size: clamp(2rem, 5vw, 3.25rem);
    line-height: 1.1;
  }

  .page-intro {
    font-size: 1.05rem;
    line-height: 1.7;
    max-width: 600px;
  }

  /* ─── Services Grid ─── */
  .services {
    padding-bottom: var(--space-2xl);
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-lg);
  }

  .service-card {
    padding: var(--space-xl);
  }

  .service-number {
    font-family: var(--font-display);
    font-size: 0.8rem;
    font-weight: 700;
    color: var(--accent);
    margin-bottom: var(--space-md);
    letter-spacing: 0.05em;
  }

  .service-card h2 {
    font-size: 1.25rem;
    margin-bottom: var(--space-sm);
  }

  .service-card p {
    font-size: 0.92rem;
    line-height: 1.65;
  }

  .service-features {
    list-style: none;
    padding: 0;
    margin-top: var(--space-md);
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .service-features li {
    font-size: 0.82rem;
    color: var(--text-secondary);
    padding-left: 1.25rem;
    position: relative;
  }

  .service-features li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.5em;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--accent);
    opacity: 0.4;
  }

  @media (max-width: 768px) {
    .services-grid {
      grid-template-columns: 1fr;
    }
  }

  /* ─── Approach ─── */
  .approach {
    padding: var(--space-xl) 0 var(--space-2xl);
  }

  .approach-content h2 {
    font-size: clamp(1.5rem, 3.5vw, 2rem);
    margin-bottom: var(--space-md);
  }

  .approach-content p {
    font-size: 1rem;
    line-height: 1.75;
  }

  /* ─── CTA ─── */
  .cta {
    padding: var(--space-xl) 0 var(--space-2xl);
  }

  .cta-inner {
    text-align: center;
    max-width: 500px;
    margin: 0 auto;
  }

  .cta-inner h2 {
    font-size: clamp(1.5rem, 3.5vw, 2rem);
    margin-bottom: var(--space-sm);
  }

  .cta-inner p {
    margin-bottom: var(--space-lg);
  }
</style>
